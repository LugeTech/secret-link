meta {
  name: post note
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/secretnotes/notes/{{phrase}}
  body: json
  auth: inherit
}

body:json {
  {
  "message": "Note content to encrypt" 
  }
}

settings {
  encodeUrl: true
}
